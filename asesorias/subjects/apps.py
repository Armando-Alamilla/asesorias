"""Subjects app."""

# Django imports
from django.apps import AppConfig


class SubjectsAppConfig(AppConfig):
    """Subjects app config."""

    name = 'asesorias.subjects'
    verbose_name = 'Subjects'
